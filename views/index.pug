extends layout

block content
  include mixins/restaurantCard
  div.container
    div.add-button-row
      a.button.save(href="/add") Add Restaurant
    each restaurant in restaurants
      +restaurantCard(restaurant)
